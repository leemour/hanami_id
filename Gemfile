# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in hanami_id.gemspec
gemspec

gem "hanami_id-generators", path: "gems/hanami_id-generators", require: false
