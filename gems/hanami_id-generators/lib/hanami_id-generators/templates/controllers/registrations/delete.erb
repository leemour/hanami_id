        before :authenticate_<%= model %>!

        def initialize(repository: <%= HanamiId.classify repository %>.new)
          @repository = repository
        end

        def call(params)
          resource = @repository.delete(params[:id])
          flash[:success] = I18n.t('hanami_id.registrations.destroyed')
          redirect_to routes.root_path
        end